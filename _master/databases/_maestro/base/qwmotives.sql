CREATE VIEW QWMOTIVES AS
SELECT 
    SYSID,
    NAME,
    DESCRIPTION,
    REGISTRY,
    TYPOLOGYID,
    DIRECTION,
    REFERENCEID,
    COUNTERPARTID,
    CONSISTENCY,
    SCOPE,
    UPDATING,
    DELETING,
    STATUS,
    DISCHARGE,
    TAG
FROM QVMOTIVES
WHERE DELETED=0
